name: show_hide_fab
description: A flutter package that provides wraps around a FloatingActionButton (FAB) in a scaffold to provide easy show/hide functionality.
version: 0.1.1
homepage: https://github.com/Akora-IngDKB/show_hide_fab.git

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  # To add assets to your package, add an assets section, like this:
  # assets:
  #  - images/a_dot_burr.jpeg
  #  - images/a_dot_ham.jpeg
  #
